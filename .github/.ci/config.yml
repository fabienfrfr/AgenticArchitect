stages:
  - test
  - deploy

test:
  stage: test
  script:
    - pip install -r apps/backend/requirements.txt
    - pytest tests/

deploy:
  stage: deploy
  script:
    - docker-compose -f infra/docker-compose.yml up --build -d
